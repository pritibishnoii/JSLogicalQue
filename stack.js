class Stack {
  constructor() {
    this.arr = [];
    this.size = 0;
  }

  push(elm) {
    if (this.size == this.arr.length) return "list is full";
    
  }

  isEmpty() {
    return (this.arr = []);
  }
}
